# AI Detection Service Dependencies
# Minimal set for HuggingFace ViT model

# PyTorch 2.2+ has better ARM64 support (no SIGSEGV on inference)
torch>=2.2.0,<2.6.0
transformers>=4.40.0,<5.0.0
pillow>=10.0.0,<11.0.0
accelerate>=0.30.0
numpy>=1.26.0,<2.0

# Web server
flask>=3.0.0,<4.0.0
gunicorn>=21.0.0,<23.0.0
